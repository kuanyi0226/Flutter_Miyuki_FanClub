import 'package:flutter/material.dart';

class YukiSekaiListPage extends StatelessWidget {
  const YukiSekaiListPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: ListView(children: [ListTile()]),
    );
  }
}
